# 欧美分级素材库

这组素材用于展示欧美电影分级，包括 `G`、`PG`、`PG-13`、`R`、`NC-17`。

素材来自 Wikipedia/Wikimedia 的 MPA block rating 文件页。`NC-17` 已替换为用户提供的 SVG 文件。文件页标注为 public domain，但这些图形包含 MPA 分级商标，实际使用仍应考虑商标限制。

后续接入详情页时，应通过 `manifest.json` 的 `western` 分组识别为“欧美分级”。
